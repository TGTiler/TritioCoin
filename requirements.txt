# TritioCoin Dependencies

# Core
argon2-cffi>=21.3.0      # Argon2id PoW hashing
ecdsa>=0.18.0             # ECDSA signatures (secp256k1)
cryptography>=41.0.0      # AES-256-GCM wallet encryption, TLS certs
mnemonic>=0.20.0          # BIP39 mnemonic backup

# Network
aiohttp>=3.9.0            # REST API + WebSocket server

# Optional (NAT traversal)
# miniupnpc>=2.0.0        # UPnP port forwarding

# Development
pytest>=7.0.0             # Test framework
pytest-asyncio>=0.21.0    # Async test support
